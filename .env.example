POSTGRES_USER=portfolio
POSTGRES_PASSWORD=portfolio_dev
POSTGRES_DB=portfolio

SECRET_KEY=change-this-to-a-random-secret-in-production

MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin
MINIO_BUCKET=portfolio

MAIN_DOMAIN=portfolio.local

# Directory to store Docker volume data (postgres, minio, node_modules, .next)
# Defaults to ./docker-volume-data if not set
# DOCKER_VOLUME_DIR=./docker-volume-data
